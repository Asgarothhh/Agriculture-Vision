{
  "name": "agrovision-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agriculture Vision web UI",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "polygon-clipping": "^0.15.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "jsdom": "^26.0.0",
    "vite": "^6.2.0",
    "vitest": "^3.0.0"
  }
}
